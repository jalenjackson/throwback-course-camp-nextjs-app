const { quizzes } = require('./quizzes/quizzes');
const { singleQuiz } = require('./quizzes/singleQuiz');
const { createQuiz } = require('./quizzes/createQuiz');

module.exports = {
  quizzes,
  singleQuiz,
  createQuiz
};
