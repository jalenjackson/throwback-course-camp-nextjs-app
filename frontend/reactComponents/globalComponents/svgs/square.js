import React from 'react';

const Square = () => (
    <div style={ styles.Square } className='square' />
);

const styles = {
  Square: {
    width: '20px',
    height: '20px',
    background: 'none',
    border: '2px solid #F6E23C'
  }
};

export default Square;
